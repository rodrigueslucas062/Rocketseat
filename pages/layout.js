const Layout = ({ children }) => {
    return (
        <html lang="pt">
            <head>
                <title>Lucas Rodrigues</title>
            </head>
            <body>
                {children}
            </body>
        </html>
    );
};

export default Layout;
